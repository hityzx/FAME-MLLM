#                🚨🚨🚨🚨🚨🚨🚨🚨🚨🚨🚨🚨🚨🚨🚨🚨🚨🚨🚨🚨🚨🚨🚨🚨🚨🚨🚨🚨🚨🚨🚨🚨🚨🚨🚨🚨🚨🚨🚨🚨🚨🚨🚨🚨🚨🚨🚨🚨
#           This file was automatically generated from src/transformers/models/qwen3_vl/modular_qwen3_vl.py.
#               Do NOT edit this file manually as any edits will be overwritten by the generation of
#             the file from the modular. If any change should be done, please apply the change to the
#                          modular_qwen3_vl.py file directly. One of our CI enforces this.
#                🚨🚨🚨🚨🚨🚨🚨🚨🚨🚨🚨🚨🚨🚨🚨🚨🚨🚨🚨🚨🚨🚨🚨🚨🚨🚨🚨🚨🚨🚨🚨🚨🚨🚨🚨🚨🚨🚨🚨🚨🚨🚨🚨🚨🚨🚨🚨🚨
# coding=utf-8
# Copyright 2025 The Qwen Team and The HuggingFace Inc. team. All rights reserved.
#
# Licensed under the Apache License, Version 2.0 (the "License");
# you may not use this file except in compliance with the License.
# You may obtain a copy of the License at
#
#     http://www.apache.org/licenses/LICENSE-2.0
#
# Unless required by applicable law or agreed to in writing, software
# distributed under the License is distributed on an "AS IS" BASIS,
# WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
# See the License for the specific language governing permissions and
# limitations under the License.

from typing import Optional, Union

import numpy as np

from ...feature_extraction_utils import BatchFeature
from ...image_utils import ImageInput
from ...processing_utils import ImagesKwargs, MultiModalData, ProcessingKwargs, ProcessorMixin, Unpack, VideosKwargs
from ...tokenization_utils_base import PreTokenizedInput, TextInput
from ...utils import logging
from ...video_utils import VideoInput


logger = logging.get_logger(__name__)


class Qwen3VLVideosProcessorKwargs(VideosKwargs, total=False):
    pass


class Qwen3VLImagesKwargs(ImagesKwargs):
    min_pixels: Optional[int]
    max_pixels: Optional[int]
    patch_size: Optional[int]
    temporal_patch_size: Optional[int]
    merge_size: Optional[int]


class Qwen3VLProcessorKwargs(ProcessingKwargs, total=False):
    images_kwargs: Qwen3VLImagesKwargs
    videos_kwargs: Qwen3VLVideosProcessorKwargs
    _defaults = {
        "text_kwargs": {
            "padding": False,
            "return_token_type_ids": False,
            "return_mm_token_type_ids": False,
        },
        "videos_kwargs": {"return_metadata": True},
    }


class Qwen3VLProcessor(ProcessorMixin):
    r"""
    Constructs a Qwen3VL processor which wraps a Qwen3VL image processor and a Qwen2 tokenizer into a single processor.
    [`Qwen3VLProcessor`] offers all the functionalities of [`Qwen2VLImageProcessor`] and [`Qwen2TokenizerFast`]. See the
    [`~Qwen3VLProcessor.__call__`] and [`~Qwen3VLProcessor.decode`] for more information.
    Args:
        image_processor ([`Qwen2VLImageProcessor`], *optional*):
            The image processor is a required input.
        tokenizer ([`Qwen2TokenizerFast`], *optional*):
            The tokenizer is a required input.
        video_processor ([`Qwen3VLVideoProcessor`], *optional*):
            The video processor is a required input.
        chat_template (`str`, *optional*): A Jinja template which will be used to convert lists of messages
            in a chat into a tokenizable string.
    """

    attributes = ["image_processor", "tokenizer", "video_processor"]
    image_processor_class = "AutoImageProcessor"
    video_processor_class = "AutoVideoProcessor"
    tokenizer_class = ("Qwen2Tokenizer", "Qwen2TokenizerFast")

    def __init__(
    self,
    image_processor=None,
    tokenizer=None,
    video_processor=None,
    chat_template=None,
    use_fava=False,
    fava_token="<|fava_pad|>",
    fava_num_tokens=128,
    fava_num_frames=16,
    fava_image_size=224,
    **kwargs,
):
        super().__init__(image_processor, tokenizer, video_processor, chat_template=chat_template)
        self.image_token = "<|image_pad|>" if not hasattr(tokenizer, "image_token") else tokenizer.image_token
        self.video_token = "<|video_pad|>" if not hasattr(tokenizer, "video_token") else tokenizer.video_token
        self.image_token_id = (
            tokenizer.image_token_id
            if getattr(tokenizer, "image_token_id", None)
            else tokenizer.convert_tokens_to_ids(self.image_token)
        )
        self.video_token_id = (
            tokenizer.video_token_id
            if getattr(tokenizer, "video_token_id", None)
            else tokenizer.convert_tokens_to_ids(self.video_token)
        )
        self.vision_start_token = (
            "<|vision_start|>" if not hasattr(tokenizer, "vision_start_token") else tokenizer.vision_start_token
        )
        self.vision_end_token = (
            "<|vision_end|>" if not hasattr(tokenizer, "vision_end_token") else tokenizer.vision_end_token
        )
        self.vision_start_token_id = (
            tokenizer.vision_start_token_id
            if getattr(tokenizer, "vision_start_token_id", None)
            else tokenizer.convert_tokens_to_ids(self.vision_start_token)
        )
        self.vision_end_token_id = (
            tokenizer.vision_end_token_id
            if getattr(tokenizer, "vision_end_token_id", None)
            else tokenizer.convert_tokens_to_ids(self.vision_end_token)
        )

        # ===== FaVA token settings =====
        self.use_fava = use_fava
        self.fava_token = fava_token
        self.fava_num_tokens = fava_num_tokens
        self.fava_num_frames = fava_num_frames
        self.fava_image_size = fava_image_size

        self.fava_token_id = tokenizer.convert_tokens_to_ids(self.fava_token)
        if self.fava_token_id == tokenizer.unk_token_id:
            self.fava_token_id = None

    def _preprocess_fava_videos(self, videos, num_frames: int = 16, image_size: int = 224):
        """
        Convert videos to FaVA inputs.

        Output:
            np.ndarray [num_videos, num_frames, 3, image_size, image_size],
            float32, RGB, values in [0, 1].

        This preprocessing must be consistent with FaVA pretraining:
            - uniformly sample 16 frames;
            - RGB;
            - resize to 224x224;
            - no mean/std normalization.
        """
        import numpy as np

        video_list = self._ensure_list_of_videos(videos)
        processed_videos = []

        for video in video_list:
            frames = self._video_to_frame_list(video)
            frames = self._uniform_sample_frames(frames, num_frames=num_frames)

            frame_arrays = []
            for frame in frames:
                img = self._frame_to_pil(frame)
                img = img.convert("RGB")
                img = img.resize((image_size, image_size))

                arr = np.asarray(img, dtype=np.float32) / 255.0
                arr = np.transpose(arr, (2, 0, 1))  # [3, H, W]
                frame_arrays.append(arr)

            video_array = np.stack(frame_arrays, axis=0)  # [T, 3, H, W]
            processed_videos.append(video_array)

        return np.stack(processed_videos, axis=0).astype(np.float32)

    def _ensure_list_of_videos(self, videos):
        """
        Normalize input into a list of videos.

        A video can be:
            - list[PIL.Image]
            - list[np.ndarray]
            - np.ndarray [T, H, W, C]
            - np.ndarray [T, C, H, W]
            - torch.Tensor [T, H, W, C]
            - torch.Tensor [T, C, H, W]
        """
        import numpy as np
        import torch
        from PIL import Image

        if videos is None:
            return []

        if isinstance(videos, np.ndarray):
            if videos.ndim == 4:
                return [videos]
            if videos.ndim == 5:
                return [videos[i] for i in range(videos.shape[0])]
            raise ValueError(f"Unsupported numpy video shape: {videos.shape}")

        if torch.is_tensor(videos):
            if videos.ndim == 4:
                return [videos]
            if videos.ndim == 5:
                return [videos[i] for i in range(videos.shape[0])]
            raise ValueError(f"Unsupported tensor video shape: {videos.shape}")

        if isinstance(videos, (list, tuple)):
            if len(videos) == 0:
                return []

            first = videos[0]

            # Single video as list of frames.
            if isinstance(first, Image.Image):
                return [videos]

            if isinstance(first, np.ndarray) and first.ndim in {2, 3}:
                return [videos]

            if torch.is_tensor(first) and first.ndim in {2, 3}:
                return [videos]

            # Batched videos.
            return list(videos)

        raise ValueError(
            "Unsupported fava_videos format. Please provide decoded frames "
            "rather than a raw video path for the current processor implementation."
        )

    def _video_to_frame_list(self, video):
        import numpy as np
        import torch
        from PIL import Image

        if isinstance(video, (list, tuple)):
            return list(video)

        if isinstance(video, np.ndarray):
            arr = video

            if arr.ndim != 4:
                raise ValueError(f"Expected video ndarray with ndim=4, got {arr.shape}")

            # [T, C, H, W] -> [T, H, W, C]
            if arr.shape[1] in {1, 3} and arr.shape[-1] not in {1, 3}:
                arr = np.transpose(arr, (0, 2, 3, 1))

            return [arr[i] for i in range(arr.shape[0])]

        if torch.is_tensor(video):
            tensor = video.detach().cpu()

            if tensor.ndim != 4:
                raise ValueError(f"Expected video tensor with ndim=4, got {tuple(tensor.shape)}")

            # [T, C, H, W] -> [T, H, W, C]
            if tensor.shape[1] in {1, 3} and tensor.shape[-1] not in {1, 3}:
                tensor = tensor.permute(0, 2, 3, 1)

            return [tensor[i] for i in range(tensor.shape[0])]

        if isinstance(video, Image.Image):
            return [video]

        raise ValueError(f"Unsupported single video type: {type(video)}")

    def _uniform_sample_frames(self, frames, num_frames: int = 16):
        import numpy as np

        if len(frames) == 0:
            raise ValueError("Cannot preprocess an empty video for FaVA.")

        if len(frames) >= num_frames:
            indices = np.linspace(0, len(frames) - 1, num_frames)
            indices = np.round(indices).astype(int).tolist()
            return [frames[i] for i in indices]

        # Repeat the last frame if the video has fewer than num_frames.
        sampled = list(frames)
        while len(sampled) < num_frames:
            sampled.append(frames[-1])

        return sampled

    def _frame_to_pil(self, frame):
        import numpy as np
        import torch
        from PIL import Image

        if isinstance(frame, Image.Image):
            return frame

        if torch.is_tensor(frame):
            frame = frame.detach().cpu().numpy()

        if isinstance(frame, np.ndarray):
            arr = frame

            # [C, H, W] -> [H, W, C]
            if arr.ndim == 3 and arr.shape[0] in {1, 3} and arr.shape[-1] not in {1, 3}:
                arr = np.transpose(arr, (1, 2, 0))

            if arr.ndim == 2:
                arr = np.stack([arr, arr, arr], axis=-1)

            if arr.ndim != 3:
                raise ValueError(f"Unsupported frame shape: {arr.shape}")

            if arr.dtype != np.uint8:
                if arr.max() <= 1.0:
                    arr = arr * 255.0
                arr = np.clip(arr, 0, 255).astype(np.uint8)

            if arr.shape[-1] == 1:
                arr = np.repeat(arr, 3, axis=-1)

            return Image.fromarray(arr).convert("RGB")

        raise ValueError(f"Unsupported frame type: {type(frame)}")
    
    def __call__(
        self,
        images: ImageInput = None,
        text: Union[TextInput, PreTokenizedInput, list[TextInput], list[PreTokenizedInput]] = None,
        videos: VideoInput = None,
        fava_videos: VideoInput = None,
        use_fava: Optional[bool] = None,
        **kwargs: Unpack[Qwen3VLProcessorKwargs],
    ) -> BatchFeature:
        """
        Main method to prepare for the model one or several sequences(s) and image(s). This method forwards the `text`
        and `kwargs` arguments to Qwen2TokenizerFast's [`~Qwen2TokenizerFast.__call__`] if `text` is not `None` to encode
        the text. To prepare the vision inputs, this method forwards the `vision_infos` and `kwrags` arguments to
        Qwen2VLImageProcessor's [`~Qwen2VLImageProcessor.__call__`] if `vision_infos` is not `None`.

        Args:
            images (`PIL.Image.Image`, `np.ndarray`, `torch.Tensor`, `list[PIL.Image.Image]`, `list[np.ndarray]`, `list[torch.Tensor]`):
                The image or batch of images to be prepared. Each image can be a PIL image, NumPy array or PyTorch
                tensor. Both channels-first and channels-last formats are supported.
            text (`str`, `list[str]`, `list[list[str]]`):
                The sequence or batch of sequences to be encoded. Each sequence can be a string or a list of strings
                (pretokenized string). If the sequences are provided as list of strings (pretokenized), you must set
                `is_split_into_words=True` (to lift the ambiguity with a batch of sequences).
            videos (`np.ndarray`, `torch.Tensor`, `list[np.ndarray]`, `list[torch.Tensor]`):
                The image or batch of videos to be prepared. Each video can be a 4D NumPy array or PyTorch
                tensor, or a nested list of 3D frames. Both channels-first and channels-last formats are supported.
            return_tensors (`str` or [`~utils.TensorType`], *optional*):
                If set, will return tensors of a particular framework. Acceptable values are:
                - `'tf'`: Return TensorFlow `tf.constant` objects.
                - `'pt'`: Return PyTorch `torch.Tensor` objects.
                - `'np'`: Return NumPy `np.ndarray` objects.
                - `'jax'`: Return JAX `jnp.ndarray` objects.

        Returns:
            [`BatchFeature`]: A [`BatchFeature`] with the following fields:

            - **input_ids** -- List of token ids to be fed to a model. Returned when `text` is not `None`.
            - **attention_mask** -- List of indices specifying which tokens should be attended to by the model (when
              `return_attention_mask=True` or if *"attention_mask"* is in `self.model_input_names` and if `text` is not
              `None`).
            - **pixel_values** -- Pixel values to be fed to a model. Returned when `images` is not `None`.
            - **pixel_values_videos** -- Pixel values of videos to be fed to a model. Returned when `videos` is not `None`.
            - **image_grid_thw** -- List of image 3D grid in LLM. Returned when `images` is not `None`.
            - **video_grid_thw** -- List of video 3D grid in LLM. Returned when `videos` is not `None`.
        """
        output_kwargs = self._merge_kwargs(
            Qwen3VLProcessorKwargs,
            tokenizer_init_kwargs=self.tokenizer.init_kwargs,
            **kwargs,
        )
        if images is not None:
            image_inputs = self.image_processor(images=images, **output_kwargs["images_kwargs"])
            image_grid_thw = image_inputs["image_grid_thw"]
        else:
            image_inputs = {}
            image_grid_thw = None

        if videos is not None:
            videos_inputs = self.video_processor(videos=videos, **output_kwargs["videos_kwargs"])
            video_grid_thw = videos_inputs["video_grid_thw"]
            # If user has not requested video metadata, pop it
            if "return_metadata" not in kwargs:
                video_metadata = videos_inputs.pop("video_metadata")
            else:
                video_metadata = videos_inputs["video_metadata"]
            video_grid_thw = videos_inputs["video_grid_thw"]
        else:
            videos_inputs = {}
            video_grid_thw = None

        # ===== FaVA video preprocessing =====
        use_fava_now = self.use_fava if use_fava is None else use_fava
        fava_inputs = {}

        if use_fava_now and videos is not None:
            if self.fava_token_id is None:
                raise ValueError(
                    f"FaVA is enabled, but {self.fava_token} is not found in the tokenizer. "
                    "Please add it to the tokenizer before using the processor."
                )

            if fava_videos is None:
                # In the common qwen_vl_utils pipeline, `videos` is already a list of decoded video frames.
                # If your `videos` are file paths, please modify qwen_vl_utils to provide fava_videos explicitly.
                fava_videos = videos

            fava_pixel_values_videos = self._preprocess_fava_videos(
                fava_videos,
                num_frames=self.fava_num_frames,
                image_size=self.fava_image_size,
            )

            if video_grid_thw is not None and fava_pixel_values_videos.shape[0] != len(video_grid_thw):
                raise ValueError(
                    f"Number of FaVA videos ({fava_pixel_values_videos.shape[0]}) does not match "
                    f"number of Qwen video inputs ({len(video_grid_thw)})."
                )

            fava_inputs["fava_pixel_values_videos"] = fava_pixel_values_videos

        if not isinstance(text, list):
            text = [text]

        text = text.copy()  # below lines change text in-place
        if image_grid_thw is not None:
            merge_length = self.image_processor.merge_size**2
            index = 0
            for i in range(len(text)):
                while self.image_token in text[i]:
                    num_image_tokens = image_grid_thw[index].prod() // merge_length
                    text[i] = text[i].replace(self.image_token, "<|placeholder|>" * num_image_tokens, 1)
                    index += 1
                text[i] = text[i].replace("<|placeholder|>", self.image_token)

        if video_grid_thw is not None:
            merge_length = self.video_processor.merge_size**2
            index = 0
            for i in range(len(text)):
                while self.video_token in text[i]:
                    metadata = video_metadata[index]
                    if metadata.fps is None:
                        logger.warning_once(
                            "Qwen3VL requires frame timestamps to construct prompts, but the `fps` of the input video could not be inferred. "
                            "Probably `video_metadata` was missing from inputs and you passed pre-sampled frames. "
                            "Defaulting to `fps=24`. Please provide `video_metadata` for more accurate results."
                        )
                        metadata.fps = 24 if metadata.fps is None else metadata.fps

                    # if timestamps are not provided, calculate them
                    curr_timestamp = self._calculate_timestamps(
                        metadata.frames_indices,
                        metadata.fps,
                        self.video_processor.merge_size,
                    )

                    video_placeholder = ""

                    # Insert one block of FaVA placeholder tokens for each input video.
                    # The actual embeddings of these placeholders will be replaced by
                    # FaVA-projected artifact tokens in Qwen3VLModel.forward.
                    if use_fava_now:
                        video_placeholder += self.fava_token * self.fava_num_tokens

                    frame_seqlen = video_grid_thw[index][1:].prod() // merge_length
                    for frame_idx in range(video_grid_thw[index][0]):
                        curr_time = curr_timestamp[frame_idx]
                        video_placeholder += f"<{curr_time:.1f} seconds>"
                        video_placeholder += (
                            self.vision_start_token + "<|placeholder|>" * frame_seqlen + self.vision_end_token
                        )
                    if f"{self.vision_start_token}{self.video_token}{self.vision_end_token}" in text[i]:
                        text[i] = text[i].replace(
                            f"{self.vision_start_token}{self.video_token}{self.vision_end_token}", video_placeholder, 1
                        )
                    else:
                        # vllm may input video token directly
                        text[i] = text[i].replace(self.video_token, video_placeholder, 1)
                    index += 1

                text[i] = text[i].replace("<|placeholder|>", self.video_token)

        return_tensors = output_kwargs["text_kwargs"].pop("return_tensors", None)
        return_mm_token_type_ids = output_kwargs["text_kwargs"].pop("return_mm_token_type_ids", None)
        text_inputs = self.tokenizer(text, **output_kwargs["text_kwargs"])
        self._check_special_mm_tokens(text, text_inputs, modalities=["image", "video"])

        if use_fava_now and video_grid_thw is not None:
            expected_fava_tokens = len(video_grid_thw) * self.fava_num_tokens
            actual_fava_tokens = 0

            for input_id_seq in text_inputs["input_ids"]:
                actual_fava_tokens += input_id_seq.count(self.fava_token_id)

            if actual_fava_tokens != expected_fava_tokens:
                raise ValueError(
                    f"FaVA placeholder token number mismatch: expected {expected_fava_tokens}, "
                    f"but got {actual_fava_tokens}. Please check whether {self.fava_token} "
                    "has been added as a special token and whether the video placeholder replacement is correct."
                )

        if return_mm_token_type_ids:
            array_ids = np.array(text_inputs["input_ids"])
            mm_token_type_ids = np.zeros_like(text_inputs["input_ids"])
            mm_token_type_ids[array_ids == self.image_token_id] = 1
            text_inputs["mm_token_type_ids"] = mm_token_type_ids.tolist()

        return BatchFeature(data={**text_inputs, **image_inputs, **videos_inputs, **fava_inputs}, tensor_type=return_tensors)

    def _get_num_multimodal_tokens(self, image_sizes=None, video_sizes=None, **kwargs):
        """
        Computes the number of placeholder tokens needed for multimodal inputs with the given sizes.
        Args:
            image_sizes (`list[list[int]]`, *optional*):
                The input sizes formatted as (height, width) per each image.
            video_sizes (`list[list[int]]`, *optional*):
                The input sizes formatted as (num_frames, height, width) per each video.
        Returns:
            `MultiModalData`: A `MultiModalData` object holding number of tokens per each of the provided
            input modalities, along with other useful data.
        """

        vision_data = {}
        if image_sizes is not None:
            images_kwargs = Qwen3VLProcessorKwargs._defaults.get("images_kwargs", {})
            images_kwargs.update(kwargs)
            merge_size = images_kwargs.get("merge_size", None) or self.image_processor.merge_size

            num_image_patches = [
                self.image_processor.get_number_of_image_patches(*image_size, images_kwargs)
                for image_size in image_sizes
            ]
            num_image_tokens = [(num_patches // merge_size**2) for num_patches in num_image_patches]
            vision_data.update({"num_image_tokens": num_image_tokens, "num_image_patches": num_image_patches})

        if video_sizes is not None:
            videos_kwargs = Qwen3VLProcessorKwargs._defaults.get("videos_kwargs", {})
            videos_kwargs.update(kwargs)
            num_video_patches = [
                self.video_processor.get_number_of_video_patches(*video_size, videos_kwargs)
                for video_size in video_sizes
            ]
            num_video_tokens = [(num_patches // merge_size**2) for num_patches in num_video_patches]
            vision_data["num_video_tokens"] = num_video_tokens

        return MultiModalData(**vision_data)

    def post_process_image_text_to_text(
        self, generated_outputs, skip_special_tokens=True, clean_up_tokenization_spaces=False, **kwargs
    ):
        """
        Post-process the output of the model to decode the text.

        Args:
            generated_outputs (`torch.Tensor` or `np.ndarray`):
                The output of the model `generate` function. The output is expected to be a tensor of shape `(batch_size, sequence_length)`
                or `(sequence_length,)`.
            skip_special_tokens (`bool`, *optional*, defaults to `True`):
                Whether or not to remove special tokens in the output. Argument passed to the tokenizer's `batch_decode` method.
            clean_up_tokenization_spaces (`bool`, *optional*, defaults to `False`):
                Whether or not to clean up the tokenization spaces. Argument passed to the tokenizer's `batch_decode` method.
            **kwargs:
                Additional arguments to be passed to the tokenizer's `batch_decode method`.

        Returns:
            `list[str]`: The decoded text.
        """
        return self.tokenizer.batch_decode(
            generated_outputs,
            skip_special_tokens=skip_special_tokens,
            clean_up_tokenization_spaces=clean_up_tokenization_spaces,
            **kwargs,
        )

    def _calculate_timestamps(self, indices: Union[list[int], np.ndarray], video_fps: float, merge_size: int = 2):
        if not isinstance(indices, list):
            indices = indices.tolist()
        if len(indices) % merge_size != 0:
            indices.extend(indices[-1] for _ in range(merge_size - len(indices) % merge_size))
        timestamps = [idx / video_fps for idx in indices]
        # @JJJYmmm frames are merged by self.merge_size, \
        # so we need to average the timestamps between the first/last frame within the temporal patch
        timestamps = [
            (timestamps[i] + timestamps[i + merge_size - 1]) / 2 for i in range(0, len(timestamps), merge_size)
        ]
        return timestamps


__all__ = ["Qwen3VLProcessor"]
